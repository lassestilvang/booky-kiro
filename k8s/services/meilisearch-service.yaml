apiVersion: v1
kind: Service
metadata:
  name: meilisearch-service
  namespace: bookmark-manager
  labels:
    app: meilisearch
    component: search
spec:
  type: ClusterIP
  clusterIP: None # Headless service for StatefulSet
  selector:
    app: meilisearch
  ports:
    - name: http
      port: 7700
      targetPort: http
      protocol: TCP
